<nav>
  <div class="nav-wrapper">
    <%= link_to "Where's Waldo?!", root_url, { class: "brand-logo", id: "logo-link"} %>
    <div class="col s12 right hide-on-med-and-down" id="breadcrumb_menu">
      <%= link_to "Worlds", root_url, { class: "breadcrumb" } %>
      <% if !@world.nil? %>
        <%= link_to @world.title, world_url(@world), { class: "breadcrumb" } %>
      <% end %>
      <% if !@highscores.nil? %>
        <%= link_to "Highscores", world_highscore_index_path(world_id: @world.id), { class: "breadcrumb" } %>
      <% end %>
    </div>
  </div>
</nav>
        